/* v27 fallback placeholder.
validation_report.html uses SVG/CSS charts and does not depend on Chart.js.
If dashboard needs real Chart.js, replace this file with the official chart.js. */
window.Chart = window.Chart || undefined;
