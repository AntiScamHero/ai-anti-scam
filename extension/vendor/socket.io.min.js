/*! v27 socket.io fallback shim. Dashboard should fall back to polling mode. */
(function(){
  function io(){
    throw new Error("socket.io fallback shim: real-time client not bundled; use polling fallback.");
  }
  io.__shim = true;
  window.io = window.io || io;
})();
